// The app-root particle layer (13.5) — public API: the canvas loop +
// emitters (particles.ts) and the React layer (particle-layer.tsx).

export * from './particles'
export * from './particle-layer'
