// Placeholder — particles + confetti.
// SDD UX §7 · WBS M13.5.
// Chip bursts, coin sparkles, cash coins, and canvas-confetti on blind clear;
// colors pulled from the active tier tokens.
export function Particles() {
  // TODO: lightweight particle layer (canvas or r3f points); canvas-confetti on clear.
  return null
}
