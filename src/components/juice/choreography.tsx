// Placeholder — scoring choreography (the 7-beat dopamine loop).
// SDD UX §6 · WBS M13.3–13.4.
// Plays over lastScore; skippable (a skip snaps to the settled end); mutates no state.
// Beats: reveal/match → tier slam → chips build → mult flare → resolve (+shake) → cash → settle.
export function ScoringChoreography() {
  // TODO: sequence the beats off lastScore; expose a skip that jumps to the end.
  return null
}
