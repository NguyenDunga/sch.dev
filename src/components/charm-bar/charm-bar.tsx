// Placeholder — charm bar with drag-to-reorder (@dnd-kit/sortable).
// SDD UX §3 · Component C7 · WBS M8 / M12.3.
// Reads RunState.charms (array order == scoring order); drag dispatches moveCharm(from, to).
export function CharmBar() {
  // TODO: sortable row of owned charm chips; neighbours slide (spring-soft) on drag.
  return null
}
