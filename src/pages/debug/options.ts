// UI Debug — the option ladders for the Button demo sections.

export const textSizes = ['xs', 'sm', 'default', 'lg', 'xl'] as const
export const iconSizes = ['icon-xs', 'icon-sm', 'icon', 'icon-lg'] as const
export const variants = [
  'primary',
  'secondary',
  'outline',
  'ghost',
  'destructive',
  'link',
] as const
