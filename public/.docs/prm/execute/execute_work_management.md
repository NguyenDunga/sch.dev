# Direct & Manage Project Work: 50/50
| Field | Value |
| --- | --- |
| **Project ID** | PRJ-2026-001 |
| **Document** | Direct & Manage Project Work |
| **Version** | 2.1 |
| **Date** | 2026-09-14 |
| **Owner** | BlueCloud (PM) |

This is the execution playbook for producing the 50/50 deliverables. It operationalizes the [Scope Management Plan](../plan/plan_scope_management.md) and the [WBS Overview](../plan/plan_wbs-overview.md) — the *what* is defined there; this file defines *how the work gets done and tracked* during execution.

> **v2.1 (2026-09-14):** checkpoint tracking **resynced** to the current WBS after the SDD alignment (engine modules per the [SDD](../../sdd/software_design_architechture.md): `scoring.ts`, `runStore.ts`, etc.) and the UX build-out (expanded M12/M13). All status is **Not started**. Module homes: pure engine in `src/core/`, the hand-phase machine + shop + save/resume in `src/state/runStore.ts`, UI in `src/pages/` + `src/components/`.

## Execution Approach

- **Solo dev loop.** One developer (BlueCloud/Qwen) executes all work. Per checkpoint: implement → `tsc` + `npm run lint` → `npm test` → `npm run dev` (playtest where relevant) → commit.
- **Milestone sequencing.** Executed in WBS order M0 → M15; a milestone isn't started until its dependencies' exit gates pass (dependencies are listed at the top of each milestone file). Gates roll up to the charter milestones via the [WBS Overview](../plan/plan_wbs-overview.md) mapping.
- **Engine-first.** The engine + store (M1–M11) is built and unit-tested before the UI (M12) and juice (M13), so presentation is a thin, skippable layer over a proven, deterministic engine.

## Work Package Tracking

One row per checkpoint, grouped by milestone. A checkpoint is `Done` only when its item in the WBS file is satisfied and the milestone exit gate is green. Update **Status** (Not started / In progress / Done) and **Completed** (date) as work is performed.

### M0 — Project Setup & Tooling → [wbs](../plan/plan_wbs-m0-setup.md)

| Checkpoint | Status | Completed | Notes |
| --- | --- | --- | --- |
| 0.1 Vite + React + TS scaffold (repo root) | Done | 2026-09-14 | Already in place; root `tsconfig.json` fixed (removed deprecated `baseUrl`/`paths`) |
| 0.2 Tailwind CSS | Done | 2026-09-14 | `@tailwindcss/vite` plugin + directives in `src/index.css` |
| 0.3 shadcn/ui (base) | Done | 2026-09-14 | `components.json` + base `button`/`card`, no custom theme |
| 0.4 @dnd-kit/sortable + core | Done | 2026-09-14 | Installed `@dnd-kit/core` + `@dnd-kit/sortable` |
| 0.5 pure-rand | Done | 2026-09-14 | Already in deps |
| 0.6 vitest + testing-library, `test` script | Done | 2026-09-14 | Installed `@testing-library/react`; `"test": "vitest run"` present |
| 0.7 Folder tree (core/state/components/pages/lib) | Done | 2026-09-14 | All folders present; tests colocated `*.test.ts` |
| 0.8 `npm run dev` renders "Hello 50/50" | Done | 2026-09-14 | Dev server serves 50/50 menu (200); SSR smoke render passes, no runtime errors |
| 0.9 `npm run build` works | Done | 2026-09-14 | Build exit 0; added missing `preview` script, `dist/` previews (200) |
| 0.10 `npm test` runs clean | Done | 2026-09-14 | Exit 0 (70 tests pass) |

### M1 — Core Data Types (`src/core/types.ts`) → [wbs](../plan/plan_wbs-m1-data-types.md)

| Checkpoint | Status | Completed | Notes |
| --- | --- | --- | --- |
| 1.1 Primitives/unions (Face, CoinEffectId, TierId, Phase, HandPhase, BossRuleId, CharmId, CharmCategory) | Done | 2026-09-14 | All unions per SDD block; counts exact (11 effect ids / 6 tiers / 4 boss rules / 5 hand phases) |
| 1.2 `Coin` (+ `faceParams`) | Done | 2026-09-14 | SDD no-null shape: `Coin { id; effects: CoinEffect[] }` — tagged-union effects carry their own params (replaces stale `CoinEffectId[]` + `faceParams?` wording) |
| 1.3 `Slot` / `Hand` / `Play` | Done | 2026-09-14 | SDD no-null shape: `HandSlot`/`FilledHandSlot` + `Hand`/`Play` (`{ kind: 'empty' }`, never null) |
| 1.4 `ShopOffer` union | Done | 2026-09-14 | `charm \| coin \| handSize` |
| 1.5 Record interfaces (Tier, Blind, CharmDef, CoinDef, Deck, Score) | Done | 2026-09-14 | All six per SDD block |
| 1.6 `RunState` | Done | 2026-09-14 | All 18 fields per SDD Run State block |
| 1.7 Single file, no logic/defaults | Done | 2026-09-14 | types-only; runtime helpers moved to `src/core/helpers.ts`; `BossRule` → balance.ts; legacy `HandState` → local to runStore.ts |
| 1.8 `tsc --noEmit` clean | Done | 2026-09-14 | tsc + lint clean; 70/70 tests green |

### M2 — Seeded RNG (`src/core/rng.ts`) → [wbs](../plan/plan_wbs-m2-rng.md)

| Checkpoint | Status | Completed | Notes |
| --- | --- | --- | --- |
| 2.1 `createRng(seed)` → next/state/restore | Done | 2026-09-14 | `restore` via pure-rand `xoroshiro128plusFromState` (4 × int32 state) |
| 2.2 `generateSeed()` | Done | 2026-09-14 | 6–8 char [A-Za-z0-9] via `crypto.getRandomValues` (global random banned in core/state) |
| 2.3 Determinism test | Done | 2026-09-14 | 100-draw deep-equal sequences |
| 2.4 Different-seed test | Done | 2026-09-14 | 100-draw sequences differ |
| 2.5 `state()`/`restore()` round-trip | Done | 2026-09-14 | mid-sequence snapshot restored into a fresh Rng; continuations exact |
| 2.6 Boolean helper 0.25 band test | Done | 2026-09-14 | `chance(rng, p)` + `intBetween(rng, min, max)` derived from `next()`; 10,000 draws in [0.22, 0.28] |

### M3 — Deck & Draw Pile (`src/core/deck.ts`) → [wbs](../plan/plan_wbs-m3-deck.md)

| Checkpoint | Status | Completed | Notes |
| --- | --- | --- | --- |
| 3.1 `buildCollection()` | Done | 2026-09-14 | Fresh module (v1.0 legacy `deck.ts`/`deck.test.ts` dropped): no-arg `buildCollection()` using `BASE_DECK_SIZE` from balance.ts; legacy store call-site updated; tsc + lint clean, 68/68 tests green |
| 3.2 `shuffleCollection(rng, deck)` | Done | 2026-09-14 | 6 tests: merge + clear discard, multiset preserved, seed-deterministic, different seeds differ, one rng draw per swap (draw-order contract pinned), input unmutated |
| 3.3 `drawFromDeck(deck)` (none on empty) | Done | 2026-09-14 | SDD C11 (source of truth) adopted: peek + `Option<Coin>`, `none` on empty — WBS “pop + `Coin | null`” wording was v1.0-era, reconciled in the plan file; 2 tests (peek order/no-consume, purity) |
| 3.4 `discardToPile` / `returnHandToPile` | Done | 2026-09-14 | 5 tests: append (draw pile untouched), hand order + empty slots count as nothing, append to existing discards, deck + hand unmutated |
| 3.5 Empty-pile → none test | Done | 2026-09-14 | `drawFromDeck` on an empty draw pile returns `none`, no throw |
| 3.6 No mid-blind reshuffle test | Done | 2026-09-14 | Drain the 5-coin pile; hand + discards grow to 5, draw pile stays empty, further draws yield `none` |
| 3.7 Shuffle clears discard test | Done | 2026-09-14 | Asymmetric 2+4 split: discard cleared, drawPile length = collection size (6) |

### M4 — Hand Phase State Machine (`src/state/runStore.ts`) → [wbs](../plan/plan_wbs-m4-hand-phase.md)

| Checkpoint | Status | Completed | Notes |
| --- | --- | --- | --- |
| 4.1 `handPhase` 5-phase cycle | Done | 2026-09-14 | Fresh 5-phase store replaces v1.0 legacy runStore (all C4 actions shipped, phase-guarded); transient `toss`/`score` are observable separate sets; 2 tests: cycle invariant via recorded phase transitions + out-of-phase no-ops. C3 stubs (`resolveFace`/`matchTier`/`scoreHand`) ship with final signatures; `HAND_SIZE`/`PLAY_SIZE` added to balance.ts; run screen placeholder until M12 |
| 4.2 `drawHand` (auto) | Done | 2026-09-14 | 5 tests: fills handSize distinct coins + → play (draw pile shrinks, discard empty); face-down = no rng consumption (rngState unchanged); short pile (3) → 3 filled + 5 empty slots; empty pile at hand start → auto-skip (handsLeft −1, no score, stays in draw) + last-hand skip ends the blind. Auto-skip is a 2026-09-14 design decision (SDD gap), recorded in SDD C4 + plan |
| 4.3 `pickCoin` / `unpickCoin` (1–5) | Done | 2026-09-14 | 7 tests: pick → next free play slot (left to right); play fills in pick order, holds 5; 6th pick no-op (coin stays in hand); unpick → first empty hand slot; re-pick round trip (unpick first, pick again); unpick on empty play slot no-op; pick on empty hand slot no-op |
| 4.4 `discard` (+ draw-enchant redraw) | Done | 2026-09-14 | 6 tests: plain coin → discard pile (draw pile untouched, gone for the blind); unlimited discards in one play phase; draw-1/2/3 redraw N face-down (discarded slot first, then left to right, parameterized); redraws capped by empty hand slots (full hand + draw2 → 1); short draw pile → fewer redraws. Enchanted coins injected via setState (base collection is all plain) |
| 4.5 `confirmPlay` | Done | 2026-09-14 | 4 tests: empty play → no-op (no phase change, no rng); → buff resting phase (toss transient, pinned in 4.1); unpicked hand coins stay in the hand (6 of 8, discard pile empty — discard happens at score); toss resolves picked faces (rng consumed, valid H/T faces) |
| 4.6 Toss auto (resolveFace per coin) | Done | 2026-09-14 | 3 tests: face set on every picked coin (empty play slots stay empty); golden-sequence test — store faces + rngState match an independent reference computation of the RNG draw-order contract (shuffle → no-rng draw → one face roll per picked coin in play order, hand coins never resolved); same seed + same picks → identical faces |
| 4.7 `echoReflip` (once per Echo) | Done | 2026-09-14 | 5 tests: re-flip rolls the rng once + sets echoUsed; second call on the same slot is a no-op (face + rngState unchanged); non-Echo coin cannot be re-flipped; two Echo coins have independent echoUsed flags (each gets exactly one); out-of-buff-phase call is a no-op. Echo coins injected via setState |
| 4.8 `score` (return hand, handsLeft−1) | Done | 2026-09-14 | 5 tests: ALL 8 hand coins (3 tossed + 5 unpicked) end in the discard pile (draw pile untouched); handsLeft −1 + → draw + hand/play emptied; C3 result applied (blindScore += total, cash += cash, lastScore = result — stub: none/0); last-hand score ends the blind (target missed → runEnd); out-of-buff-phase score is a no-op |
| 4.9 Out-of-phase no-op test | Done | 2026-09-14 | Full matrix: each of the 7 actions fired in every resting phase except its own (14 combinations, parameterized) — state unchanged; plus all 7 actions no-op in the menu phase (no run in progress) |
| 4.10 Full-cycle test | Done | 2026-09-14 | 2 tests: a full blind (10 cycles) runs without getting stuck — per-cycle coin conservation (every coin in exactly one place), draw pile −8 / discard pile +8 per cycle, machine loops back to draw, handsLeft −1 per hand; after hand 10 the deck is fully consumed and the blind ends (target missed → runEnd); same seed + same choices → identical blind (discard order, blindScore, cash, rngState, phase) |
| 4.11 Hand/play empty after score test | Done | 2026-09-14 | Focused conservation test: after score, hand and play are completely empty, the 8 coins that were in the hand (2 tossed + 6 unpicked) are exactly the discard pile's contents, and the draw pile is untouched |

### M5 — Pattern / Tier Matching (`src/core/scoring.ts`) → [wbs](../plan/plan_wbs-m5-tier-matching.md)

| Checkpoint | Status | Completed | Notes |
| --- | --- | --- | --- |
| 5.1 `matchTier(play, boss)` reads non-null faces | Done | 2026-09-14 | 3 tests: HHT.H → threeSame (empty slots count as nothing); H.H.H → tripleRun (empty slots do not break runs — pattern read on the face sequence); HHHH. → fourRow (not jackpot) |
| 5.2 threeSame | Done | 2026-09-14 | HHTH → threeSame (smallest count where it can win — at 3 coins HHH is shadowed by tripleRun) |
| 5.3 fourSame | Done | 2026-09-14 | HHHTH → fourSame (≥4 of a face, non-adjacent; at 4 coins HHHH is shadowed by fourRow) |
| 5.4 jackpot (5-same) | Done | 2026-09-14 | HHHHH → jackpot |
| 5.5 fourRow (4-in-a-row) | Done | 2026-09-14 | HHHH → fourRow |
| 5.6 alternating | Done | 2026-09-14 | HTHTH → alternating (exactly 5 strictly alternating) |
| 5.7 tripleRun | Done | 2026-09-14 | HHH → tripleRun |
| 5.8 Priority resolution | Done | 2026-09-14 | 5 tests: HHHHH → jackpot (never fourRow/fourSame/tripleRun); HHHHT → fourRow over fourSame; HTHTH → alternating over threeSame (H 3×); HHHTH → fourSame over tripleRun; HHTT → none |
| 5.9 ≤2 slots → null | Done | 2026-09-14 | 0/1/2 filled slots (5 shapes) → none |
| 5.10 Boss rules (noAlternating / noJackpots) | Done | 2026-09-14 | noAlternating: HTHTH → none (explicit override, no fall-through to threeSame); non-alternating unaffected. noJackpots: HHHHH → fourSame (fixed demotion, not fourRow); HHHHT still fourRow. shortFuse/heavyTarget do not affect tiers |
| 5.11 One case per tier at min | Done | 2026-09-14 | Matrix: tripleRun 3, threeSame 4, fourRow 4, fourSame 5, jackpot 5, alternating 5 (smallest count where each tier can win); plus below-minimum no-match cases (HHT, HHTT → none) |
| 5.12 2-coin → null test | Done | 2026-09-14 | All 4 two-coin plays (HH/HT/TH/TT) → none |

### M6 — Scoring Pipeline (`src/core/scoring.ts`) → [wbs](../plan/plan_wbs-m6-scoring.md)

| Checkpoint | Status | Completed | Notes |
| --- | --- | --- | --- |
| 6.1 Tier step | Done | 2026-09-14 | scoreHand step 1 = matchTier(play, boss); tests: HHTH → threeSame 15×1; noJackpots demotes HHHHH to fourSame through the tier step |
| 6.2 Base step (null → 0/0) | Done | 2026-09-14 | chips/mult from TIERS[tier]; no tier → { kind: 'none', cash } with no chips/mult/total |
| 6.3 Boosters step (left→right) | Done | 2026-09-14 | plusChips +10 / plusMult +1 / jackpotFever ×2 chips (jackpot tier only); magnitudes as new balance.ts constants (PLUS_CHIPS_BONUS, PLUS_MULT_BONUS, JACKPOT_FEVER_MULT); extraHand/payday ignored |
| 6.4 Score = chips × mult | Done | 2026-09-14 | boosters applied before multiplying: (15+10)×(1+1)=50; all-three-on-jackpot (50+10)×2 chips, 5 mult → 600; no-tier hand total 0 |
| 6.5 Coin cash step | Done | 2026-09-14 | $1 per Tax coin (deterministic) + $4 per Jackpot coin passing its 25% roll via chance(rng, JACKPOT_CHANCE); threshold proven (0.24 passes, 0.25/0.26 fail); merged coin pays both effects |
| 6.6 Charm-order test | Done | 2026-09-14 | [plusChips, jackpotFever] → 480 vs [jackpotFever, plusChips] → 440 on HHHHH — totals differ |
| 6.7 No-charm test | Done | 2026-09-14 | all 6 tiers with no charms: total = TIERS chips × mult for each |
| 6.8 Deterministic Tax test | Done | 2026-09-14 | counting-rng spy: Tax-only scored hand consumes 0 rng draws, pays $3 |
| 6.9 Reproducible Jackpot cash test | Done | 2026-09-14 | same seed → identical cash (5 Jackpot coins); over 20 seeds the 25% roll produces both paying and non-paying hands |
| 6.10 Null-tier zero-score test | Done | 2026-09-14 | 2-coin play with Tax + Jackpot coins → { kind: 'none' }, no total, but coin cash still pays ($5 roll-pass / $1 roll-fail) |
| — 32-hand EV test (baseline pin, per WBS note) | Done | 2026-09-14 | exhaustive enumeration of all 32 five-coin patterns through scoreHand: EV = 1870/32 = 58.4375, expected computed from TIERS × baseline distribution (not hardcoded); matches plan_balance-baseline.md corrected 2026-09-12. M4 store-test "stub" comments updated (assertions unchanged — those plays are ≤2 coins → no tier) |

### M7 — Coin Effects (`src/core/scoring.ts` + `balance.ts` + store) → [wbs](../plan/plan_wbs-m7-coin-effects.md)

| Checkpoint | Status | Completed | Notes |
| --- | --- | --- | --- |
| 7.1 List the 9 (comment block) | Done | 2026-09-14 | Comment block at the top of scoring.ts listing all 9 effects grouped face (weight/doubleSide/chaos/magnetic/reverse) / cash (tax/jackpot) / draw+re-flip (draw/echo) |
| 7.2 Face effects (odds priority) | Done | 2026-09-14 | resolveFace: magnetic (75% toward left face; falls through to next priority when left is empty) > doubleSide (100/0) > chaos (uniform 0–100% odds, 2 rng draws) > weight (75/25) > base (50/50); roll; Reverse inverts after the odds stage. Favoured face read from the effect variant (post-faceParams types) |
| 7.3 Cash effects (Tax/Jackpot) | Done | 2026-09-14 | Already landed in M6 (scoreHand step 5): Tax +$1 deterministic, Jackpot +$4 on 25% rng roll — proven by M6.5/M6.8/M6.10 tests |
| 7.4 Draw-enchant (store discard) | Done | 2026-09-14 | Already landed in M4.4: discard on a draw-N coin redraws N face-down (discarded slot first) — proven by M4.4 tests |
| 7.5 Echo (store echoReflip) | Done | 2026-09-14 | Already landed in M4.7: one re-flip per Echo coin re-runs resolveFace, echoUsed gate — proven by M4.7 tests (now exercising the real M7 resolution) |
| 7.6 Merge stacks effects | Done | 2026-09-14 | store mergeCoin(fromId, toId): shop-phase action; toId gains all of fromId's effects (stack, no cap), fromId removed from the collection (either pile), free. 4 tests: basic merge + free, two face effects stack, source in discard pile, no-ops (wrong phase / unknown id / self-merge) |
| 7.7 9 isolated effect tests | Done | 2026-09-14 | Isolated tests: weight, doubleSide, chaos, magnetic, reverse, base (scoring.test.ts M7 block); tax, jackpot (M6.5/M6.8); echo, draw (M4.7/M4.4) |
| 7.8 Merged-coin test | Done | 2026-09-14 | weight+doubleSide merged coin resolves by priority (doubleSide wins); weight+tax merged coin resolves its face and still pays tax cash in scoreHand; store-level two-face-effect stack test |
| 7.9 No out-of-scope effects | Done | 2026-09-14 | grep of src/ for the 16 future effect names (Shapeshift…Insurance): zero matches (one "mirrors" comment false-positive) |

### M8 — Charms (`balance.ts` + `scoring.ts` + store) → [wbs](../plan/plan_wbs-m8-charms.md)

| Checkpoint | Status | Completed | Notes |
| --- | --- | --- | --- |
| 8.1 `CHARMS` pool (5, categories) | Done | 2026-09-14 | Already in place from M1: CHARMS = the 5 defs (plusChips/plusMult/extraHand/payday/jackpotFever) with categories flip\|scoring\|pattern\|economy and baseline prices; pinned by balance.test.ts (5-charm pool test) |
| 8.2 `charms[]` is the ordering source | Done | 2026-09-14 | RunState.charms: CharmId[] (types.ts, M1) — array order is the only ordering source, no index field; scoring iterates it left→right (M6) |
| 8.3 `moveCharm(from, to)` | Done | 2026-09-14 | Store action: pure array move (splice out + splice in), allowed in run/shop phases (charm bar visible on both per SDD C7); no-ops on from===to, out of range, other phases. 2 tests: reorder round trip + no-op matrix |
| 8.4 No-duplicates via `includes` | Done | 2026-09-14 | Nothing to build in M8 — the `charms.includes(id)` ownership check lives in the M9 shop buy/offer logic (noted for M9) |
| 8.5 Removed charms absent | Done | 2026-09-14 | grep of src/ for Weighted Coin / Double-Sided / Always Heads / Re-Toss: zero definitions or types (only removal-history comments) |
| 8.6 Reorder-affects-scoring test | Done | 2026-09-14 | Store-level with deterministic pre-resolved plays: Jackpot HHHHH [plusChips, jackpotFever] → 480, after moveCharm(0,1) → 440 (ties to M6.6); commutative pair plusChips+plusMult on threeSame → 50 in both orders |

### M9 — Shop (`src/state/runStore.ts`) → [wbs](../plan/plan_wbs-m9-shop.md)

| Checkpoint | Status | Completed | Notes |
| --- | --- | --- | --- |
| 9.1 Offer generation (5, no owned charm) | Done | 2026-09-14 | generateOffers (runStore): pool = unowned charms + all 11 coin effects + hand-size (while under cap); Fisher–Yates sample of 5 (no in-shop duplicates); wired into endBlind (shop entry). 5 tests: exactly 5, no owned charm, pool membership, seed determinism, no hand-size at cap |
| 9.2 `reroll` (once) | Done | 2026-09-14 | Store action: shop phase + !rerollUsed → regenerate all 5 offers (rng), rerollUsed = true; second call no-op (offers and rng state unchanged). 3 tests |
| 9.3 `buy` (deduct/add, reject) | Done | 2026-09-14 | Store action: price from CHARMS/COIN_EFFECTS/HAND_SIZE_PRICE; reject (state unchanged) when broke, charm owned, or wrong phase; charm → cash −= price, charms +, offer removed (structural match — immer drafts break reference equality). 4 tests |
| 9.4 `buy` coin rolls faceParams | Done | 2026-09-14 | purchasedEffect: Weight/Double-Side roll favoured face via rng on purchase (fixed for the run); Draw-N → { kind: 'draw', count: N }; plain → unit variant; new coin (unique id max+1) appended to the draw pile. 5 tests incl. same-seed → same rolled face |
| 9.5 `mergeCoin` (free) | Done | 2026-09-14 | Already implemented in M7 (mergeCoin store action: effects stack, source removed, cash unchanged) with 4 tests — nothing new to build |
| 9.6 `removeCoin` ($1 delete) | Done | 2026-09-14 | Store action: shop phase, cash ≥ 1, coin in either pile → delete, cash −= REMOVE_COIN_COST; no-ops when broke/unknown/wrong phase. 4 tests |
| 9.7 Hand-size upgrade (cap) | Done | 2026-09-14 | buy handSize: handSize +1 (cash −= HAND_SIZE_PRICE $10), rejected at HAND_SIZE_CAP (10); offer generation omits it at cap (9.1). 2 tests |
| 9.8 Reroll-once / owned-charm-rejected tests | Done | 2026-09-14 | Covered by the 9.2 tests (second reroll no-op, rng state unchanged) and the 9.3 test (owned charm → state unchanged) |
| 9.9 Remove-$1 / no-sell tests | Done | 2026-09-14 | Covered by the 9.6 tests: $1 from either pile, cash only goes down, and an explicit assertion that no sellCharm/removeCharm action exists on the store |

### M10 — Blind / Round / Boss Progression (`balance.ts` + store) → [wbs](../plan/plan_wbs-m10-progression.md)

| Checkpoint | Status | Completed | Notes |
| --- | --- | --- | --- |
| 10.1 `BLINDS[12]` table | Done | 2026-09-14 | Already in place from M1: 12 entries, per-round small<big<boss escalating (300→3500), rewards $4/$6/$10 — pinned by balance.test.ts against the balance-baseline table |
| 10.2 `BOSS_RULES[4]` (boss only) | Done | 2026-09-14 | Already in place from M1: 4 rules, one per round; the `Blind` type puts `rule` only on the `boss` variant (no optional field on small/big); balance.test.ts asserts rules appear only on boss blinds |
| 10.3 `leaveShop` blind-start setup | Done | 2026-09-14 | Store action: blindIndex +1, round synced, handsLeft = 10 (SHORT_FUSE_HANDS 8 into the Short Fuse boss; +1 with Extra Hand), blindScore/hand/play reset, whole collection reshuffled into the draw pile (discard cleared), shop cleared, phase run. 4 tests |
| 10.4 `endBlind` met/missed | Done | 2026-09-14 | Target met → cash += reward (+$5 Payday, +$5 Heavy Target) → shop (or runEnd win after blind 11); missed → runEnd lose. Heavy Target: table target ×1.5 at runtime via HEAVY_TARGET_MULT (3500 → 5250, per SDD data). 4 tests |
| 10.5 Blind 11 cleared → win | Done | 2026-09-14 | 5250 on blind 11 → phase runEnd, won = true, $10 + $5 bonus paid; 5249 → lose. 2 tests |
| 10.6 Boss-rule-scope test | Done | 2026-09-14 | Store-level: HTHTH scores 105 on round-1 small/big but 0 on the noAlternating boss; HHHHH scores 200 on round-3 small/big but 60 (4-same) on the noJackpots boss |
| 10.7 Game-over test | Done | 2026-09-14 | Last hand ends the blind below target with $50 cash → runEnd, won = false, cash untouched (cash is not a substitute for the target) |
| 10.8 Full-progression test | Done | 2026-09-14 | Mocked-clears walk of all 12 blinds: each clear → shop with 5 offers → leaveShop → next blind (round synced, blindScore reset, handsLeft > 0); blind 11 → runEnd win; no undefined transition |

### M11 — Save / Load (`src/state/runStore.ts`) → [wbs](../plan/plan_wbs-m11-save-load.md)

| Checkpoint | Status | Completed | Notes |
| --- | --- | --- | --- |
| 11.1 `save()` ({version:2,state}, explicit) | Done | 2026-09-14 | Store action: run/shop phases only; writes { version: 2, state } to localStorage key 'fifty-fifty-run' (incl. rngState + coin collection). 3 tests: shape/fields, explicit-only, menu no-op |
| 11.2 `resume()` (null/absent no-op) | Done | 2026-09-14 | Store action: absent / unparseable / v1 (non-migratable) / non-run-shop phase → no-op; valid v2 → restores seed, round/blind, cash, charms + order, collection, handSize, runScore, rngState (rng restored before any draw). 4 tests |
| 11.3 Resume in `run` (blind start) | Done | 2026-09-14 | Reset: hand/play/handPhase/lastScore/blindScore, handsLeft to the blind's initial budget (10; 8 on Short Fuse; +1 Extra Hand), whole collection re-reshuffled from the restored rngState (discard cleared). 3 tests |
| 11.4 Resume in `shop` (same offers) | Done | 2026-09-14 | Lands at the shop; offers regenerated deterministically from the restored rngState (two fresh resumes of one save → identical offers); rerollUsed preserved (a spent reroll stays spent). 2 tests |
| 11.5 Round-trip deep-equal test | Done | 2026-09-14 | save → resume in a fresh store: every persisted field deep-equals the saved state; the collection's coins and their effect payloads survive (piles compared as a set — re-reshuffled at blind start) |
| 11.6 No-autosave test | Done | 2026-09-14 | Spy on localStorage.setItem across a full walk (3 hands → clear → shop: buy/reroll/merge/remove/moveCharm → leaveShop → next-blind hand → toMenu): never called |

### Quality Gate — NASA Coding Practices (pre-M12, user-directed 2026-09-14)

| Checkpoint | Status | Completed | Notes |
| --- | --- | --- | --- |
| QG.1 No function > 60 lines | Done | 2026-09-14 | Refactored `createRunStore` (344 → module-level action functions) and `Coin` (94 → `CoinBadges` + `SpinningCoin` subcomponents). Then split the store into action modules — `handActions.ts` (hand machine), `shopActions.ts` (shop + progression), `saveActions.ts` (save/resume), `storeTypes.ts` (plumbing types); `runStore.ts` is now state shape + wiring only (567 → 143). `runStore.test.ts` (1918) split into per-milestone suites: `handFlow` / `charms` / `shop` / `progression` / `saveLoad` + shared `testHelpers.ts`. `debug.tsx` (dev-only) excluded. Enforced by ESLint `max-lines-per-function: 60` (tests + debug page excluded) |
| QG.2 100% test coverage (logic layer) | Done | 2026-09-14 | `src/core` + `src/state` at 100% statements/branches/functions (497/497, 298/298, 117/117). 8 new edge tests close the last branches (generated seed, draw1/draw3 variants, both favoured faces, drawHand skip, full-hand unpick, empty-slot discard, non-resumable resume). Enforced by vitest `thresholds: {100: true}` on `src/core/**` + `src/state/**`; UI files join the gate when M12 lands component tests |

### M12 — UI Screens, Theme & Interaction → [wbs](../plan/plan_wbs-m12-ui.md)

| Checkpoint | Status | Completed | Notes |
| --- | --- | --- | --- |
| 12.1 Theme tokens + lucide (no-blur-shadow) | Done | 2026-09-14 | Ceramic Tactile tokens in `index.css` `:root` (palette, tier colors, radii 8/14/22, motion, shadow rule); Tailwind `@theme`/`@theme inline` + shadcn semantic tokens wired to them; Fredoka display font (score numerals) + Geist UI; lucide-react (already present). Re-themed button (coral/teal/ink, 2px ink sticker border), coin faces (flat — no radial gradients), score ticker. **Shadow audit: no blurred drop shadows anywhere** — only hard offsets (`3px 3px 0 var(--ink)` style) + coral glow bloom on the CTA pulse (glow allowed). Also fixed 17 pre-existing `tsc -b` errors (HandSlot narrowing in handFlow.test via a `filled()` helper) so the build is green |
| 12.2 App phase router + transitions | Done | 2026-09-14 | `App.tsx` renders the screen for `RunState.phase` (menu/run/shop/runEnd) via a phase→screen map; `AnimatePresence mode="wait"` + `motion.div` keyed on phase plays the screen-in/out (UX §5: slide 24px + fade, `SPRING.soft` ≈ 300ms) on every phase change; `useReducedMotion` degrades it to a plain cross-fade (UX §8). `overflow-x-clip` on body guards the slide-in |
| 12.3 Menu (C5) | Done | 2026-09-14 | `menu.tsx`: title + tagline; seed field (6–8 chars, `maxLength=8`, blank = random) with a random-seed button (`generateSeed`, Dices icon); New Run (primary, pulse, form submit — Enter works); Resume (secondary) rendered only when `hasSave()` is true. New store action `hasSave()` (saveActions) — a resumable-save query via `parseSave` (the store stays the only localStorage layer); read once on mount via a lazy `useState` initializer. 1–5-char seed → invalid hint + disabled New Run. 5 new tests (absent / valid / unparseable / v1 / non-resumable phase) |
| 12.4 Coin component + interaction states | Done | 2026-09-14 | Replaced the M3 tap-to-toss `coin.tsx` with the SDD C6 model: `hand-coin.tsx` (face-down disc + effect badges; hover lift 4px + pointer tilt max 8° written straight to the DOM, press 2px, disabled desaturated/no-offset/not-allowed, focus-visible 3px coral ring), `play-slot.tsx` (empty = sunk dashed well with index; filled = seated coin, face-down until the toss, then face + face badge; tap → unpick), `coin-disc.tsx` (flat back "?" / H gold / T slate-blue disc + `FaceBadge` H/T pill — glyph primary, color secondary, colorblind-safe), `coin-badges.tsx` (effect badges, extracted). Pick/unpick spring via shared `layoutId` (`SPRING.snappy` hand→play, `SPRING.soft` play→hand); 6th-pick (play full) → 3px shake (CSS keyframes, `shakeKey` remount restarts it; error sfx in M13). Transform layers compose without fighting: framer `transform` on the button, CSS `translate`/`rotate` on the inner tilt div. `run.tsx` now hosts the hand + play area (auto-draw effect on `handPhase==='draw'`, pick/unpick handlers, revealed = toss/buff/score) — the rest of C6 lands in 12.5/12.6. coin.css rewritten for the new model (old SpinningCoin styles removed) |
| 12.5 3D coin (lands on Slot.face) | Done | 2026-09-14 | **Deviation from the SDD's r3f+rapier:** built with **CSS 3D transforms** instead — fiber v9 won't install on React 19.3 (peer caps `<19.3`), and CSS is inherently flat + unlit, so it upholds "never reads as realistic" with zero WebGL/WASM and a jsdom-testable path (user chose this over a React downgrade or `--legacy-peer-deps`). `toss-coin.tsx`: a two-face disc (H front, T back, `preserve-3d` + `backface-visibility`) that arcs up (`translateY` keyframe), tumbles (`rotateX`, 2 spins + face offset — H→720°, T→900°), and settles on the resolved face; the tumble finishes exactly at landing (420ms), then a small bounce + settle (700ms total). Staggered left→right (50ms/coin) so the last coin lands at ~900ms (UX §4). The outcome is **never animation-derived** — the face is already resolved in the store (confirmPlay); the animation only provides the arc + tumble + settle feel. `useReducedMotion` → a 2D cross-fade of the face (~160ms), no arc/tumble/3D, same landing face. `play-slot.tsx` now renders the `TossCoin` when revealed (toss/buff/score) and the 2D face-down coin (tappable) in play. `toss-coin.css` added (imported in index.css). No new deps; 216/216 tests, build + lint green |
| 12.6 Run screen (draw/play/toss/buff/score) | Done | 2026-09-14 | Full C6 hand flow wired in `run.tsx` (thin layer over the store). **Blind header** (`blind-header.tsx`): round + name (Small/Big/Boss + boss rule), target (Heavy Target ×1.5 at runtime), running blindScore (turns green when met), hands left, draw-pile + discard-pile counts. **Discard** (unlimited): a Discard mode toggle in the action bar — in discard mode a hand coin is discarded on tap (draw-enchant coins show their ↻N redraw pip via the existing badge); the hand row tints red in discard mode. **Confirm** (play→toss, explicit, pulses when ≥1 in play) and **Score** (buff→score, explicit, pulses) — no auto-timer. **Echo re-flip** (buff): an unused Echo coin in the play is tappable → `echoReflip`; the TossCoin gains a `quick` mode (single full spin + small arc, ~400ms, no stagger) and re-mounts on key change (face+echoUsed) so it re-tosses. **Charm bar** (C7, `charm-bar.tsx`): dnd-kit sortable row of owned charms in scoring order, drag → `moveCharm(from,to)` (PointerSensor, 4px activation). **Score ticker** (chips×mult=total) wired to `lastScore`. Split `run.tsx` to stay under the 60-line function limit: `useHandFlow` hook (pick/discard/shake/confirm), `ActionBar` component, `canReflipAt` pure helper. `run.css` added. 216/216 tests, build + lint green |
| 12.7 Shop screen (C8) | Done | 2026-09-14 | C8 shop in `shop.tsx` (thin layer). **Offer grid** (`offer-card.tsx`): 5 cards (charms / special coins / hand-size upgrade) with name + one-line description (`descriptions.ts`) + price; hover raises 2px with a hard offset + a tier glow ring (a bloom, not a drop shadow — UX §2); the Buy button is disabled when broke or unavailable (charm owned / hand size at cap) — the store also re-rejects. **Reroll** (free, disabled after use). **Charm bar** (C7, shared with the run screen). **Collection** (`collection.tsx`): every owned coin (draw + discard pile) with its effect badges; **Merge** is a two-step selection (tap "Merge" on a source → it highlights, then "Merge into" a target → `mergeCoin`; "Cancel" clears); **Remove** ($1, disabled when broke). **Cash** in the header. **Leave** (primary, pulse) → `leaveShop`. `shop.css` added. 216/216 tests, build + lint green |
| 12.8 Run-end (C9) | Done | 2026-09-14 | C9 run-end in `run-end.tsx` (thin layer). Win/lose heading + subtitle (round, blind kind, final blindScore/target). **Run summary** (`run-summary` dl): blinds cleared (all 12 on a win, else the index of the blind lost), **runScore** (total score across the run), final cash. Seed shown in a code chip (for sharing/replay). New Run (primary, pulse) + Menu. **runScore was missing from the state** (the SDD data model defines it but M10/M11 never accumulated it): added `runScore += scoreTotal` in `score` (handActions) + a handFlow test proving it accumulates across hands. `run-end.css` added. 217/217 tests, build + lint green |
| 12.9 Manual Save button | Done | 2026-09-14 | `SaveButton` (save-button.tsx): explicit save only (no autosave, SDD UX). Taps the store's `save()` (serializes to localStorage) and shows a brief "Saved!" confirmation (1.5s, timeout cleaned up on unmount). Placed top-right on the run screen, next to the blind header (`.run-top` row). Extracted the play area into a `PlayArea` component to keep RunScreen under the 60-line function limit. 217/217 tests, build + lint green |
| 12.10 Tactile controls pass (all states) | Done | 2026-09-14 | Tactile controls pass (SDD UX §3, §8). Audited every interactive element (Button, HandCoin, PlaySlot, re-flip button, CharmChip, OfferCard Buy, Collection Merge/Remove, SaveButton, menu seed field + random button): all have default/hover/active/disabled/selected/focus-visible (selected = aria-pressed variant for Discard, dragging state for CharmChip, pick spring for coins). **Hit targets ≥44px**: bumped Button xs/sm/md/lg from 24/28/32/36px to 44px (h-11; xl stays 48px) and CharmChip to min-height 44px (coins 64px, slots 72px, re-flip 64×83px, menu seed 44px already met). **Focus-visible = 3px coral ring** on every element. **Keyboard path**: every interactive element is a `<button>`/`<input>` (focusable by default; tab order: coins → confirm/score → shop). **ARIA labels** on coins ("Pick coin N (effects)"), slots ("slot N, unpick" / "re-flip slot N"), charms ("Name, drag to reorder"), seed field (sr-only label). **OS text scaling**: functional sizes use rem (scale with OS text); only decorative shadows use px. 217/217 tests, build + lint green |
| 12.11 Screen smoke tests | Done | 2026-09-14 | `screens.smoke.test.tsx` (jsdom + @testing-library/react, `// @vitest-environment jsdom` docblock): each screen renders from a valid RunState without throwing. **Menu**: title + New Run, no Resume (no save). **Run**: blind header + 5 play slots + 8 hand coins + Confirm (from a drawn hand). **Shop**: title + 5 offer cards + Leave (from a shop phase with offers). **Run-end (win)**: "Run complete!" + runScore. **Run-end (lose)**: "Game over" + blinds cleared (3/12). Installed `jsdom` (dev dep). The jsdom localStorage (Node --localstorage-file) lacks the standard API, so the suite stubs it with the in-memory `makeLocalStorage` helper (same as the saveLoad suite) in a `beforeAll`. 222/222 tests (217 + 5 smoke), build + lint green |

### M13 — Juice (Choreography, Particles, Shake & Sound) → [wbs](../plan/plan_wbs-m13-juice.md)

| Checkpoint | Status | Completed | Notes |
| --- | --- | --- | --- |
| 13.1 Deal/pick/discard motion | Done | 2026-09-15 | UX §5 motion on the Run screen, all presentational (UX §0 — juice never mutates state). **Piles** (`piles.tsx`): deck stack showing the drawPile count (visual origin of the `deal` flight) + discard well (sunk dashed circle, Trash2 icon) showing the discardPile count (target of the `discard` ghost), flanking the hand row in a `.piles-row`. **Deal** (`deal.ts` + `useDeals` in run.tsx): pure `computeDealIndex` — a coin in neither the previous hand nor the previous play gets a 0-based stagger index in slot order (unit-tested in `deal.test.ts`: fresh hand, seated coins, unpick return, draw-enchant redraw, empty slots); prev-ids are state adjusted during render (the React "store information from previous renders" pattern — no effect, so deals are visible in the very render the coins mount). `hand-coin.tsx` plays the flight on a separate `motion.div` layer (220ms ease-out from the deck side, 40ms stagger per index) so the shake remount never replays it; an unpick return (no `dealIndex`) gets `initial={false}` and the shared-layout crossfade plays instead. **Pick/unpick**: `play-slot.tsx` springs in with `SPRING.snappy` (crossfade plays on the mounting element); the hand coin's layout transition is `SPRING.soft`. **Discard** (`discard-ghost.tsx`): the store removes the coin instantly; a fixed ghost (portaled to `<body>` so viewport coords survive the screen-in/out transform) flies from the tapped coin's rect to the well center + fades — 180ms ease-out, self-removes on `onAnimationComplete`, `pointer-events: none` (never blocks input). **Reduced motion** (UX §8): deal = quick fade, no flight/stagger; discard = fade in place, no flight. `EASING` tokens in `lib/motion.ts` are now framer bezier tuples (CSS keeps its `--ease-out`/`--ease-back` vars). `charm-chip.tsx` extracted from charm-bar (react-refresh). Tests: `deal.test.ts` (pure) + `run.juice.test.tsx` (jsdom: pile counts, discard → ghost + pile move, draw-enchant redraw dealt — 1 of 2 lands on a full hand per the M4.4e cap). 231/231 tests, tsc + lint green |
| 13.2 Toss + Echo flip (spring-bouncy) | Done | 2026-09-15 | UX §5 toss/echo motion on the M12.5 CSS-3D plumbing, all presentational (UX §0 — the face is store-resolved; the animation always lands on `Slot.face`). **Toss** (`toss-coin.tsx`): the two-face disc (H front / T back, `backface-visibility`) plays a two-phase arc via framer `useAnimation` — rise (ease-out, `TOSS.rise` 0.4s, apex −64px) then a `SPRING.bouncy` settle whose overshoot past the rest line IS the landing bounce (no baked keyframe bounce); the tumble (rotateX, 2 full spins, `TOSS.tumble` 0.51s easeInOut) is timed to finish exactly when the coin first lands (rise + the spring's first zero-crossing). **Echo** (12.6 re-flip): one spin + a smaller arc (`ECHO.rise` 0.15s, apex −32px, ~500ms), no stagger; the coin re-mounts on the re-flip (key = id + face + echoUsed) so it re-tosses rather than face-swaps. **Stagger**: initial toss staggers left→right (`index * TOSS.stagger` 50ms); echo never staggers. **Landing math** (`toss.ts`): pure `settleRotation(face, spins)` = whole spins + face offset (H 0°, T 180°) — unit-tested (H 0 mod 360, T 180 mod 360 for 1 & 2 spins) and asserted in the DOM (the flip's `aria-label` matches `Slot.face`). **Reduced motion** (UX §4/§8): the existing 2D cross-fade (~160ms) — no arc, no tumble, no 3D. `TOSS`/`ECHO` timing constants in `lib/motion.ts`. Tests: `toss-coin.test.tsx` (jsdom: landing math, flip disc both faces + label, reduced-motion cross-fade via a `matchMedia` stub) + `run.juice.test.tsx` (jsdom integration: confirmPlay → 5 toss coins each on the store-resolved face; forced Echo coin → re-flip re-mounts the coin on its new face, `echoUsed` set). 238/238 tests, tsc + lint green |
| 13.3 Scoring choreography (7 beats) | Done | 2026-09-15 | UX §6 7-beat dopamine loop over `lastScore`, all presentational (UX §0 — reads the store's Score, never mutates state, never gates an action, always skippable). **Pure logic** (`choreography.ts`): `takeSnapshot` (the play + charms captured at the Score tap — the store empties the play synchronously), `matchedIndices` (per-tier matched pattern: jackpot/alternating = all, fourRow/tripleRun = longest run, threeSame/fourSame = first N of the majority face), `bannerText` ("JACKPOT!" / tier name / "No match"), `TIER_RANK`/`isBigHit` (rank ≥ 3 = big hit), `beatDurations` (per-beat ms scaling with score size, capped at the ~2.4s budget; reduced motion = the ≤150ms fast beats), `cashCoinCount` (Tax/Jackpot payers), `chipCount` (matched coins + scoring charms, capped at 12). **Sequence machine** (`use-scoring-choreography.ts`): starts when `lastScore` changes (a stale lastScore at mount never replays); arms the beat timers 2→7 (beat 1 is the present); a tap/key anywhere during beats 1–6 skips to beat 7 (the settled end) and changes no number; stale run ids never advance the sequence. **Overlay** (`scoring-choreography.tsx`, portaled to `<body>` like the 13.1 ghost): beat 1 the coin row pops in (70ms stagger, the matched pattern glows in the tier color, the others dim); beat 2 the tier banner slams in (scale 0.6→1 ease-back 220ms) + a 120ms tier-color flash; beat 3 the flying chips (one per contributing coin/charm, coin row → chips counter); beat 4 the mult flare (pulse + grow, color ramps to `--primary`); beat 5 the resolve (chips/mult collide, the total counts up, `--primary` flash on big hits); beat 6 the cash coins (Tax/Jackpot payers fly to the cash counter); beat 7 the settled end (the overlay is gone, the numbers rest in the ticker). **Beat-aware ticker** (`score-ticker.tsx`): the counters build on their beats — chips tick on 3, the mult flares on 4, the total counts up on 5, the cash lands on 6; the row remounts per run id (each hand's counters start from zero); a skip snaps the counters to the final values instantly. **Reduced motion** (UX §8): the fast beats (≤150ms), no flights/flares, instant-ish counters. Screen shake (13.6), sfx (13.7), particles/confetti (13.5) are stubbed out for their checkpoints. Tests: `choreography.test.ts` (pure: snapshot, matched pattern per tier, banner text, rank/big-hit, durations incl. the cap + reduced, cash/chip counts) + `run.juice.test.tsx` (jsdom integration: Score → banner + chip flights + the ticker settles on the store numbers; a mid-sequence tap skips to the settled end with no number change; a no-tier hand plays "No match" + the cash lands; reduced motion = fast beats, no flights). 259/259 tests, tsc + lint + build green |
| 13.4 Skip/fast-forward (no number change) | Done | 2026-09-15 | The skip mechanism shipped with 13.3 (a tap/key anywhere during beats 1–6 → `skip()` clears the beat timers and snaps to beat 7; the ticker's counters snap to their final values instantly via the `skipped` flag); this checkpoint is the systematic verification that a skip changes **no** number (UX §0/§6: "a second tap during the sequence fast-forwards to step 7 instantly"). **Verification** (`run.juice.test.tsx`, 8 new tests): (1) a **key** (Enter) during the sequence skips to the settled end with the store numbers intact — the 13.3 test covered pointer only; (2) **the core check** — the same hand (same seed → same deck) scored twice, once let to play out and once skipped at the banner: the full settled store state is **deep-equal** (blindScore, runScore, cash, handsLeft, round, blindIndex, lastScore, won, phase, handPhase, hand, play, charms, deck drawPile/discardPile, even `rngState` — a skip calls no store action, so it consumes no RNG); (3) a tap **right after the Score tap** fast-forwards from beat 1 (the overlay never becomes visible); (4) repeated taps/keys (fast-forward spam) are idempotent; (5) a tap **after** the sequence ends changes nothing; (6) skipping a **cash** hand (Tax) counts the cash exactly once — the cash is settled by `score()` before the sequence starts, so a skip neither loses nor double-counts it; (7) a skip **never blocks input** (UX §9) — the auto-drawn next hand is in the play phase and a pick works immediately after the skip; (8) skip works under reduced motion (fast beats). No source changes were needed — the 13.3 beat machine already satisfies the spec; `fourRowRun`/`noTierTaxRun` helpers were hoisted to module scope to share between the 13.3 and 13.4 suites. 267/267 tests, tsc + lint + build green |
| 13.5 Particles + confetti | Done | 2026-09-15 | Canvas particle layer (UX §7), purely presentational (UX §0 — pointer-events: none, never blocks input). **Engine** (`particles.tsx`): module-level particle pool + a rAF loop that runs **only while particles are alive** (no idle cost, UX §9), DPR cap 2, dispose on unmount. `emitBurst` (radial, chip burst 6–12 / coin sparkle ~4), `emitConfetti` (200–300 rect pieces falling from the top, blind clear). Colors from the tier/theme tokens: `resolveColor` maps CSS vars to hex (getComputedStyle with a hex fallback so jsdom/no-stylesheet still resolves) — canvas can't paint `var()`. **Wiring**: `ParticleLayer` + `useBlindClearConfetti` mounted once at the app root (`App.tsx` `JuiceLayer`, portaled-free fixed canvas, persistent across screens so the confetti outlives the run screen); the hook fires confetti exactly on the store's run → shop transition (endBlind). Beat bursts in `scoring-choreography.tsx` (`BeatBursts`): chip burst (tier color) at the chips counter on beat 3, cash burst at the cash counter on beat 6. Landing sparkle (~4, face color) in `toss-coin.tsx` on the first landing of the arc. **Reduced motion** (UX §8): minimal — counts ×0.3, lifetimes ×0.6. `.particle-layer` CSS in choreo.css (fixed, z-60, pointer-events none). Tests: `particles.test.tsx` (jsdom, stubbed 2d ctx + rAF: burst lives/draws/dies and the loop stops when the last particle dies; reduced = fewer; confetti 200–300; color resolution per tier; blind-clear confetti exactly on run→shop; unmount disposes). 274/274 tests, tsc + lint green |
| 13.6 Screen shake (0 on reduced-motion) | Done | 2026-09-15 | `ScreenShake` (UX §7): translate the shaken root a few px with **exponential decay** (offset = amp · e^(−4t/T), 250–350ms), amplitude scaled by tier. **Engine** (`screen-shake.ts`): `shakeScreen({amplitude, duration})` trigger + a rAF loop that runs **only while a shake is active** (no idle cost, UX §9); `attachShakeEl` attaches/detaches the shaken element (detach cancels an in-flight shake — no rAF leak). **Zero under prefers-reduced-motion** (UX §8): the trigger checks the media query and no-ops. **Amplitudes** (`choreography.ts`): `SHAKE_AMPLITUDE` per tier — threeSame 2 · tripleRun 3 · fourSame 4 · alternating 5 · fourRow 6 · **jackpot 8** — and `CLEAR_SHAKE_AMPLITUDE` 10 (target clear). **Wiring**: the app root wraps the screens in `ScreenShake` (`App.tsx`) so both shakes move the whole screen — the resolve shake fires on beat 5 of the scoring choreography (`BeatBursts`, tier-scaled; a no-match hand shakes nothing), and the target-clear shake (10px, 350ms) fires in `BlindClearConfetti` on the run → shop transition (where the run screen is already unmounting, hence app-root). Purely presentational (UX §0) — reads no state, never gates an action. Tests: `screen-shake.test.tsx` (jsdom, multi-callback rAF stub: decay → settle → loop stops; **zero under reduced motion**; zero-amplitude no-op; unmount cancels in-flight shake; amplitude table per UX §7 incl. escalation; beat 5 shakes at the tier amplitude; a no-match score shakes nothing). 281/281 tests, tsc + lint green |
| 13.7 Sound map (howler, no music) | Done | 2026-09-15 | `sfx.ts` (UX §10): the **16-event sound map** played through **Howler** (no music). **Assets**: 16 short synthesized mono 16-bit PCM .wav (22.05kHz) in `public/resource/sfx`, generated by `scripts/generate-sfx.mjs` (generated ⇒ CC0, no external assets; README documents the set). **Design** (UX §9/§10): **lazy** (Howls created on the first play — always after a user gesture), **concurrency cap** of 8 simultaneous voices (extra plays dropped — fast chip ticks never pile up), **ducking** (a big event — tierHit / winStinger / loseStinger — ducks the small ones ×0.4 for 300ms), **rate** options (±5% for toss/land, rising chip ticks, tier-rank pitch for the tier hit). Missing asset or blocked AudioContext ⇒ silence, never an error (UX §0). **Wiring**: `Button` plays its `sfx` prop on click (default `button`; shop reroll → `reroll`, buy → `buy`); run: `deal` (auto-draw), `pick`/`unpick`, `discard`, `error` (6th pick), staggered `toss` per coin on confirm (timers cleared on unmount); `land` on the toss-coin landing (also in the reduced-motion crossfade — reduced motion affects motion, not audio); choreography: `tierHit` (beat 2, pitch by tier rank), rising `chip` ticks (beat 3, one per flying chip), `mult` (beat 4), `cash` clinks (beat 6, one per cash coin; tick timers cleared on beat change); `winStinger` on the run → shop blind-clear transition; `loseStinger` on the run-end screen (loss only — a win already got the stinger). Tests: `sfx.test.ts` (mocked Howler via `vi.hoisted` + `vi.resetModules` per test: the 16 events of the UX table each with a base volume; src/volume/rate; **concurrency cap** 8 + slot freeing; **ducking** 300ms window + big events unducked; missing asset silenced, no retry). 288/288 tests, tsc + lint green (refactored `useHandFlow`/`RunScreen`/`RunEndScreen` back under the 60-line limit: `useDiscardGhost`, `useTossSfx`, `useUnpickSfx`, `useLoseStinger`) |
| 13.8 Reduced-motion / a11y path | Done | 2026-09-15 | The UX §8 pass. **Reduced motion** (`prefers-reduced-motion: reduce`): the app root is wrapped in `<MotionConfig reducedMotion="user">` (`App.tsx`) — framer disables every transform/layout animation (deal flights, pick/unpick springs, screen slides) for such users, keeping only opacity/color changes; the 3D toss is already a 2D cross-fade (`toss-coin.tsx`), the screen shake is a no-op (13.6), particles are scaled to 30% with shorter TTLs (13.5), counters tick on the ≤150ms fast beats (13.3), and the **hover tilt is off** — the JS pointer tilt in `hand-coin.tsx` is gated on `useReducedMotion`, and the CSS hover lifts (hand coin, reflip slot, charm chips, offer cards) + the 6th-pick shake animation are disabled under `@media (prefers-reduced-motion: reduce)` (color-only feedback stays: badge brightening, glow rings). Same final state and numbers in every case. **A11y**: full keyboard path — the hand coin, the unpick slot, the reflip slot, the discard toggle, Confirm/Score, and every shop/menu button are real `<button>`s (Tab + Enter/Space); the choreography skip listens to `keydown` as well as pointer (13.3); focus-visible 3px coral rings on all interactive elements; ARIA labels on coins (`Pick coin N (effects)`), slots (`slot N, unpick`), charms (`name, drag to reorder`, `role=list`), and the H/T faces (`heads`/`tails`); **colorblind-safe H/T** — the glyph (H·T) is the primary signal, color secondary, never color alone; hit targets ≥ 44px (hand coin 56px, play slot 64×83px); rem-based sizing respects OS text scaling. **Bug fix found by the tests**: the hover tilt wrote *two angles* to the CSS `rotate` property (single-angle) — invalid, silently ignored everywhere; it now uses `transform: rotateX() rotateY()` on the tilt div (plus a zero-rect guard). Tests: `hand-coin.test.tsx` (real `<button>` + ARIA label; click/Enter picks; the tilt follows the pointer; **no tilt under reduced motion**; H/T glyph + ARIA, never color alone). 293/293 tests, tsc + lint green |
| 13.9 Perf (60fps, code-split, non-blocking) | Done | 2026-09-15 | The UX §9 performance audit. **rAF hygiene**: exactly two rAF loops in the codebase — the particle engine and the screen shake — and both run **only while active** (the particle loop stops the frame the last particle dies; the shake loop stops at decay completion and is cancelled on unmount) — **no idle rAF cost** (tested: `particles.test.tsx` stray-frame no-op, `screen-shake.test.tsx` loop-stops/unmount-cancels). **DPR cap 2** on the particle canvas (a 3× phone draws at 2× — tested both the cap and the under-cap case). **60fps**: all juice is compositor-friendly — CSS 3D transforms/opacity only (no layout-triggering properties animated), the hover tilt is written straight to the DOM (no re-render per pointermove), the particle canvas is one `clearRect` + batched draws per frame with a 50ms dt clamp, confetti is a single 240-piece batch (200–300, tested). **Non-blocking**: every juice layer is `pointer-events: none` (particles, choreo overlay, shake wrapper) — input is never blocked, and the choreography is always skippable (tap or key, 13.3). **Audio**: howler with the concurrency cap (8) + ducking (13.7) — no clip pile-ups on fast play. **Code-split / three**: the r3f/rapier code-split requirement is **N/A by the M12.5 deviation** — there is no three.js/r3f/rapier in the bundle at all (CSS 3D coins), so the Run screen pays no 3D engine cost; the whole app ships as one 524K minified JS chunk (React + framer-motion + base-ui + zustand + howler), well under what the r3f path would have cost. **Encoding fix**: `screen-shake.tsx` had been written cp1252 by a non-utf8 script during 13.6 (the production build failed on it) — re-encoded to UTF-8; a repo-wide scan confirms all sources are valid UTF-8. 295/295 tests, tsc + lint + `vite build` green |

### M13a — Layout, Interaction Overhaul & Rebalance → [wbs](../plan/plan_wbs-m13a-layout.md)

> **M13a (2026-09-15):** interaction overhaul + rebalance layered on M12/M13. Numbers in [balance-baseline](../plan/plan_balance-baseline.md); mechanics in [SDD Component Design](../../sdd/software_design_component.md). 13a.1–13a.12 **Done** (2026-09-15/16).

| Checkpoint | Status | Completed | Notes |
| --- | --- | --- | --- |
| 13a.1 Auto-advance the buff phase (skip no-Echo buff) | Done | 2026-09-15 | UI flow convenience (same pattern as the auto-draw — `useAutoScore` in `run.tsx`), the store is untouched. When the buff phase opens with **no unused Echo re-flip** in the play, the run scores itself after the last tossed coin lands (`(n−1)·TOSS.stagger + TOSS.rise + 150ms`) by calling the same `handleScore` the button calls (snapshot + `score`). A hand with an available re-flip is never auto-scored (the player may want to re-flip first), and the explicit Score button stays as a fast-forward (a double score is a no-op — the store only scores from `buff`). Tests: `run.13a.test.tsx` (a no-Echo hand never stops in `buff`; an unused-Echo hand waits for input and the Score button still works; **auto vs. manual score changes no number** — same seed, identical blindScore/runScore/cash/lastScore/handsLeft/discard). 298/298 tests, tsc + lint green |
| 13a.2 Keep unplayed coins in hand (replace dump-all) | Done | 2026-09-15 | The dump-everything rule is gone. `scoreDraft` (`handActions.ts`) now sends **only the played coins** to the discard pile; **unplayed hand coins stay in the hand** and the next `drawHand` refills to `handSize` around them (it already fills empty slots). `returnHandToPile` is deleted from `deck.ts` (and its 3 tests). **Collection-conservation fixes the change exposed**: `leaveShopDraft` and `resume` both reset the hand to empty — under keep-unplayed that silently dropped the unplayed coins, so both now merge the in-hand coins back into the collection first (the blind-start reshuffle / shop persistence then includes them). M4 conservation tests re-pinned: M4.8 (only played coins discarded, hand keeps its unplayed), M4.10 (1-coin-per-cycle drain: draw pile loses 8 then 1/cycle — the deck is no longer consumed over a blind), M4.11 (post-score state). Save/load tests updated: the restored collection is the saved collection **plus** the saved hand's coins. Per-blind drain on the future 24-deck will be ≈23 draws (balance-baseline). 295/295 tests, tsc + lint green |
| 13a.3 Rebalance: 4 hands, 24 mixed deck, halved targets | Done | 2026-09-15 | Per balance-baseline: `HANDS_PER_BLIND` 10→4, `SHORT_FUSE_HANDS` 8→3, `BASE_DECK_SIZE` 80→24 (open decision resolved: keep 24 — keep-unplayed drain is ≈23 draws/blind, so 24 covers it with a 1-coin buffer; hand-size upgrades can deck out on the last hand, accepted cost). `buildCollection` → 16 plain + 8 Weight(favored Heads) via new `STARTER_WEIGHT_COINS` constant — aligned favored faces (opposing faces cancel under a face-down draw; balance-baseline critical finding). `BLINDS` targets halved (150/250/400 · 300/500/750 · 500/800/1200 · 750/1200/1750); Heavy Target comment 1750→2625. Re-pinned: balance.test.ts (tables + constants), deck.test.ts buildCollection (mixed deck), progression.test.ts (target-met/miss/Heavy/win values: 150/149/2625/2624), run.juice.test.tsx (deck-count aria: 16/15), handFlow M4.10 title (4 cycles). One seed re-probe: the Weight face-roll coverage test pins rng seeds, and the 80→24 shuffle changed the rng stream — probe-0/probe-2 → probe-5(H)/probe-0(T). 295/295 tests, tsc + lint green |
| 13a.4 Convert leftover hands to money on early clear | Done | 2026-09-15 | `scoreDraft` now ends the blind the **moment `blindScore ≥ target`** (new `effectiveTarget(blind)` helper — Heavy Target ×1.5 included), not just when the hand budget runs out. `endBlind` pays `handsLeft × EARLY_CLEAR_BONUS_PER_HAND` (new constant, draft **+$1/hand**) on top of base + Payday + Heavy Target, and records it in a new `RunState.earlyClearBonus` field (0 on a full-budget clear). The shop screen shows the reward breakdown: "Blind cleared! +$X (+$Y early-clear (Z unused hands))" — deterministic, visible, `role=status`. `earlyClearBonus` resets on `startRun` and `leaveShop`. **Save format bumped v2 → v3** (new field; v2 saves discarded, same non-migratable policy as v1 → v2) — saveLoad tests re-pinned incl. a v2-discarded case. 5 new tests: clear on hand 2 of 4 (+$2), clear on last hand (+$0), Payday stacking, early clear on the final blind (win + bonus), miss plays out the full budget. 300/300 tests, tsc + lint green |
| 13a.5 Drag-and-drop + multi-select coins | Done | 2026-09-15 | Pointer drag (dnd-kit `PointerSensor`, 6px activation so plain clicks stay clicks): press-drag a hand coin onto the play row to pick it — dropping a **selected** coin picks the whole selection (in hand order, capped at free slots). Multi-select: Ctrl/Cmd+click toggle, Shift+click range from anchor, Ctrl/Cmd+A select-all, Ctrl/Cmd+number toggle, click-empty-space / Esc clear (selection is render-state only — UX §0, never touches the store). **Play-row reorder**: each face-down play coin is draggable (`play-i`) and every slot a drop target (`slot-i`) → new `movePlayCoin(from, to)` store action (move onto empty, swap onto filled; play phase only) — enables order-dependent tiers (tripleRun / alternating / fourRow). **Double-click quick-play**: pick + auto-confirm when the row fills (dblclick caught on the hand row — the coin animates away; a 350ms unpick guard swallows the 2nd click landing on the mid-flight coin). Keyboard: 1–0 pick nth coin, Enter confirm, Space score. `routeDrop` (pure) routes drag-end; `CoinDnd`/`DraggableHandCoin`/`PlayDropZone`/`PlaySlotDnd` components in `coin-dnd.ts(x)` (engine/component split for react-refresh). 11 new tests (routeDrop ×5, movePlayCoin ×4, quick-play ×2). 329/329 tests, 100% coverage on gated scope, tsc + lint green |
| 13a.6 Drop-zone score & discard areas | Done | 2026-09-16 | The Discard mode toggle is gone (`action-bar.tsx` — no more `discardMode` state): a plain tap always picks. The discard well (`DiscardWell`, `piles.tsx`) is wrapped in an always-live droppable (`DiscardWellDrop`, dnd-kit id `discard-well`, dashed ring while a coin hovers) — `routeDrop` (pure, `coin-dnd.ts`) routes a hand coin dropped on the well to a new `discard` route, and the run wires it to the **same store `discard`** every other path calls (UX §0); dropping a **selected** coin discards the whole selection (unlimited — no cap, `discardTargets`). **Quick-discard hotspot**: a corner chip in the hand zone (`QuickDiscardChip`, ≥44px, sticker style) — hovering it discards the **hovered** coin (pointer handoff, 120ms grace window in `useHoveredCoin`), else the whole **selection** (click works too — the no-pointer path: select, Tab, Enter). **Per-coin D key**: `HandCoin` handles D while focused (aria label gains “press D to discard”) — the keyboard discard path for no-pointer users; the draw-enchant redraw pip still shows (store logic, tested). The discard ghost (13.1) plays the flight from the coin’s element (registered via `registerRef`) to the well. `discardTargets`/`selectedInOrder` (pure, `coin-dnd.ts`). 18 new tests: `coin-dnd.test.tsx` (routeDrop discard ×2, discardTargets ×4, selectedInOrder ×1, D-key ×2), `run.13a6.test.tsx` (toggle gone + well droppable; D-key discard + store invariants; D-key draw-enchant redraw; D-key no-op outside play; hotspot hover handoff; hotspot selection discard; hotspot no-op; hotspot absent outside play); `run.juice.test.tsx` discard tests re-pinned to the D key. 347/347 tests, tsc + lint green |
| 13a.7 Pre-computed score + auto-end score phase | Done | 2026-09-16 | **Live projected total** (`projectScore`, pure, `scoring.ts`): the deterministic pipeline (M6 steps 1–4: tier → base → boosters → total) over the first `landed` tossed coins — the deterministic part of `scoreHand` was extracted to `tierScore` so projection and real score share one code path (M13 §0: no number changes auto vs manual); coin cash (the Jackpot 25% roll) stays with the real score only (projection `cash` = 0). `landDelay` (pure, `toss.ts`): when a coin first lands — the arc rise after the left→right 13.2 stagger; reduced motion: all coins at 160ms. `useTossProjection` (`run.tsx`): on a fresh toss (phase → `buff`) it resets the landed count and schedules each coin’s landing; the ticker shows a live **“projected k/n”** sticker + the emerging tier name — the counters animate from their previous value (no remount, so the build-up reads as accumulation, not a flash). `ScoreTicker` gains a projection mode (`score-ticker.tsx` + CSS): while the toss is live it shows the projected chips × mult = total instead of the previous hand’s settled score (flat “projected” sticker, dashed border, Ceramic Tactile — no blur). **Auto-end**: the 13a.1 auto-score condition already means “nothing left to interact with” (no *available* Echo re-flip) — so an Echo hand now scores itself after its last re-flip; the delay was extended +150ms → +900ms after the last coin lands so the player can read the final projection before the choreography starts. The next hand auto-advances as before (the store’s score→draw is synchronous + the auto-draw). The **Score button is unchanged** — explicit early end / fast-forward (Space shortcut; the choreography skip is the fast-forward). No store changes (UX §0). 11 new tests: `scoring.test.ts` (projectScore ×6: pattern build-up, matches scoreHand’s deterministic part, boss rules, empty-slot compaction, clamp, no cash roll), `toss-coin.test.tsx` (landDelay ×2), `run.13a7.test.tsx` (ticker updates without a click + final score matches the projection + projection gone after scoring; an Echo hand auto-scores after its last re-flip; the manual Score button still ends early). 359/359 tests, tsc + lint green |
| 13a.8 Rework the shop layout | Done | 2026-09-16 | **Collection-first** (`collection.tsx` rewritten): the “Your deck” panel is the shop’s visual anchor — every owned coin (draw + discard pile) as a tile: drag handle (the `CoinDisc`, dnd-kit draggable) + effect badges + Merge / Remove ($1) buttons; the whole tile is a drop target. **Drag-to-merge**: drop coin A on coin B → the store `mergeCoin(A, B)` (UX §0 — same action as every other path); a `DragOverlay` copy follows the pointer, the source tile dims, the hovered target gets the dashed ring. The **two-step tap flow** (tap “Merge” on the source → “Merge into” on the target, “Cancel” clears) stays as the no-pointer / keyboard fallback — buttons are plain buttons (the drag handle is the disc, so taps never start a drag). **Shop order** (`shop.tsx`): header (title + cash) → reward → **Collection** → offers **grouped by kind** (Charms / Coins / Hand size — `groupOffers`, pure) → CharmBar → **sticky footer** with Reroll (“free, once” → “Rerolled”) and Leave (nudge: “Leaving with $X — it carries over” when cash > 0). `offer-card.tsx` gains a `poor` state (dimmed card + dimmed price — unaffordable) and a category-color top border per kind. `shop.css`: collection tiles, offer sections, sticky footer, poor/drag states (Ceramic Tactile — flat stickers, no blur; ≥44px targets). 10 new tests: `shop.13a8.test.tsx` (collection renders first; offers grouped by kind; Reroll label; Leave nudge; poor state), `collection.13a8.test.tsx` (drag-merge → store merge; tap fallback; cancel; remove charges $1; self-drop no-op). dnd-kit in jsdom: collision hit-tests the **drag-overlay copy’s** rect, so element rects are mocked at the `Element.prototype` level (tiles + overlay); dnd-kit swallows a drag’s trailing click with a document capture listener removed 50ms after the drag — `afterEach` waits it out so the next test’s clicks aren’t stopped. 369/369 tests, tsc + lint green |
| 13a.9 Show each coin's favored face | Done | 2026-09-16 | The 75/25 value only exists if the player can see which face is favored (balance-baseline critical finding) — so a Weight / Double-Side coin now shows its favored face **on its badge everywhere the coin appears**: `CoinBadges` (shared by the hand coin, the pre-toss play slot, the toss coin, the discard ghost, the drag copy and the shop collection) renders the existing `FaceBadge` (H/T pill — glyph first, face color second, colorblind-safe UX §8) right after the W / DS badge, grouped so the tooltip covers both (“Weight — 75% chance of Heads”). Shop offers can’t show a face (the favored face is rolled at purchase, SDD C8) — the one-liners now say so (“75% chance of a favoured face (rolled at purchase)”). **Pre-existing bug found while wiring this**: `CoinDisc` / `FaceBadge` generated `--h` / `--t` class suffixes but the CSS keys off `--heads` / `--tails` — the face color had silently never applied anywhere (the disc showed a bare H/T glyph on a transparent background). Fixed at the source (`faceClass` in `coin-disc.tsx`), so the resolved-face colors in the play row and the new favored-face badges both read on the ceramic theme. 8 new tests: `coin-badges.test.tsx` (W+H, W+T, DS+T, plain effects unchanged, merged coin shows both faces, tooltip names the face, empty renders nothing), `hand-coin.test.tsx` (a Weight(H) hand coin shows the face badge; the H/T disc test now also pins the `--heads` / `--tails` classes). 377/377 tests, tsc + lint green |
| 13a.10 First-run onboarding hint | Done | 2026-09-16 | A dismissable sticker banner on the run screen explains the three core gestures of the 13a input model (“Drag a coin to the play row to pick it · drag it into the bin to discard · match a pattern to score”). Shown only on the first run: the flag lives in its own localStorage key (`fifty-fifty-onboarded`, `src/lib/onboarding.ts` — a UI preference, deliberately NOT in the save file) and is set on first show and on dismiss, so the hint never comes back. Never blocks input: the banner is `position: fixed` (an overlay — showing/dismissing it can’t shift the layout, the “no layout shift” accept) with `pointer-events: none`; only the 44px × button (UX §3 hit target) is live. Entrance is a plain fade, disabled under `prefers-reduced-motion` (UX §8). Purely presentational — the store is untouched (UX §0). 8 new tests: `onboarding-hint.test.tsx` (first-run show + the three gestures, flag persisted on first show, hidden once flagged, × dismiss persists, pointer-events CSS contract) + `run.13a10.test.tsx` (on the real run screen: first run shows the overlay, dismiss removes it for good, later runs never show it). 385/385 tests, tsc + lint green |
| 13a.11 Re-tune Extra Hand / Short Fuse for 4-hand blind | Done | 2026-09-16 | Headless playtest of the **real store** (`src/core/playtest-13a11.test.ts` — face-blind greedy policy: every hand subset 1–5 evaluated by expected score over all face assignments, 200 runs/blind; EV math cross-checked against `scoreHand`/`resolveFace` so the sim can’t drift from the engine). **Playtest note — clear rates with/without Extra Hand (base 24-coin collection, no discards, no buys):** 1s(150) 83.5→86.5%, 1b(250) 54.0→63.0%, 1B(400) 10.0→14.0%, 2s(300) 37.5→43.0%, 2b(500) 0.5→2.5%, 2B(750) 0.0→0.0%, 3s(500) 2.0→6.5%, 3b–4B 0.0→0.0% (4h→5h); full run (no buys) 0.0% both variants — the base game is a build-gate by design (“from blind 3 the build must carry”). **Decisions:** Extra Hand **$10→$15** in `balance.ts` (+1 hand ≈ +5–9pp on the mid blinds that matter, 1.4–3× on the hard ones — the only charm adding a full hand of EV, worth more than $10). Short Fuse **stays at 3 hands**: the round-2 boss (750) has a structural wall — 3×200 (max tier) = 600 < 750, so 3 hands can’t clear without boosters (4×200 = 800 is the only no-booster path); with a booster it’s a jackpot-gate (1.3% with Jackpot-Fever + 4 Weight-H), not impossible — the boss’s difficulty is set by the target, not the hand count. 5 new tests (2 cross-checks + 3 playtest cells), 390/390 tests, tsc + lint green |
| 13a.12 Keyboard & reduced-motion parity for new input | Done | 2026-09-16 | The no-pointer paths + reduced-motion degradation were built incrementally (13a.5 shortcuts, 13a.6 D-key + quick-discard, 13.8 `MotionConfig reducedMotion="user"`, 13.1–13.3 reduced-motion juice) — this checkpoint proves the three acceptance criteria in `src/pages/run.13a12.test.ts`: **(1) keyboard-only full hand** — number keys pick, D on a focused coin discards, Enter confirms, Space scores (the same store actions as the pointer paths, UX §0); **(2) reduced-motion drops instant + same result** — app root `MotionConfig reducedMotion="user"` degrades every transform/layout animation (pick/unpick springs, deal flights, screen slides); the discard ghost degrades to a fade in place (no flight) via the new pure `ghostMotionProps` (split to `discard-ghost.ts`, coin-dnd pattern); the store result of a hand played under reduced motion is byte-identical to the same hand (same seed) played without it; **(3) hit targets ≥44px** — coin 56px, play slot 83px, quick-discard 44px, charm chip 44px, discard well 52px (CSS contract test). 5 new tests, 396/396 tests, tsc + lint green |

**M13a exit gate — click-count per hand, before/after (vs M12):**

| Action | M12 (before) | M13a (after) | Saved |
| --- | --- | --- | --- |
| Pick a coin | 1 click/coin | 1 drag/coin (or 1 number-key; multi-select drag = 1 gesture for N coins; double-click quick-play) | multi-select: N→1 |
| Discard N coins | **2 + N clicks** (Discard-mode toggle ON + N + toggle OFF) | **N** (drag-to-well / D-key / quick-discard hotspot — **no mode toggle**) | **2 per discard op** |
| Score a no-Echo hand | **1 click** (forced Score button) | **0** (auto-advance, 13a.1/13a.7; Space / Score = optional fast-forward) | **1 per hand** |

Net: a typical hand that discards once and has no Echo now costs **3 fewer interactions** than M12 (2 from the toggle, 1 from the forced Score), and multi-select collapses N picks into 1 drag. No Discard-mode toggle, no forced Score click on a no-Echo hand, auto-advance idle phases — all landed (13a.1, 13a.6, 13a.7).

**M13a exit-gate status (2026-09-16):** all 12 checkpoints Done; `tsc`/`lint`/`npm test` green (396/396); balance tests re-pinned (13a.3), M4/M8 conservation updated for keep-unplayed (13a.2); no engine mutation from any input-convenience layer (drag/drop/auto-advance all route through existing store actions — UX §0, tested); M13a rows Done with dates. **Pending (manual):** a full 4-blind run played *entirely by drag* and *entirely by keyboard* reaching the same scores — the keyboard path is fully tested (13a.12), the drag path is tested (13a.5/13a.6), and the same-result property is proven (13a.12 store-identical under reduced motion + the shared store actions), but a full end-to-end manual playtest on the new curve is a hands-on check (note: the base game is a build-gate by design — 13a.11 — so a full clear requires shop builds, not just input).

### M14 — Test Suite Completion → [wbs](../plan/plan_wbs-m14-tests.md)

| Checkpoint | Status | Completed | Notes |
| --- | --- | --- | --- |
| 14.1 Every core module + store has a test | Not started | | |
| 14.2 Full suite 100% green | Not started | | |
| 14.3 Determinism regression (draw-order + state/restore) | Not started | | |
| 14.4 Boundary sweep (k × tiers) | Not started | | |
| 14.5 No `Math.random` in core/state | Not started | | |

### M15 — README & Final Packaging → [wbs](../plan/plan_wbs-m15-readme.md)

| Checkpoint | Status | Completed | Notes |
| --- | --- | --- | --- |
| 15.1 README install/run | Not started | | |
| 15.2 README seed entry/share | Not started | | |
| 15.3 Build playable end-to-end | Not started | | |
| 15.4 `npm test` 100% | Not started | | |
| 15.5 Out-of-scope audit | Not started | | |

## Deliverables Produced

Each milestone produces the deliverables named in the [Scope Statement](../plan/plan_scope-statement.md). Record actual production here.

| Deliverable | Milestone | Produced | Verified | Notes |
| --- | --- | --- | --- | --- |
| Local web build (full 12-blind run) | M15 | | | |
| Source code (Vite + React + TS) | M0–M15 | | | |
| vitest suite (scoring + RNG + state) | M2–M14 | | | |
| README (run + seed sharing) | M15 | | | |
